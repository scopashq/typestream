# Necessary because npm otherwise completely ignores gitignored files even if
# they are explicitly listed under `files` in the `package.json` file
# See: https://npm.github.io/publishing-pkgs-docs/publishing/the-npmignore-file.html
